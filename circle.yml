dependencies:
  post:
    - pyenv local 2.7.9 2.6.8
  override:
    - pip install tox

test:
  override:
    - tox -e flake8
#        parallel: true
    - tox -e py27
#        parallel: true
    - tox -e py26
#        parallel: true

